import type Pose from '../../armature/Pose';
import type { IKChain } from '../rigs/IKChain';
import { quat, vec3 } from 'gl-matrix';
import SwingTwistBase from './support/SwingTwistBase';
import { Transform } from '../../maths';
declare class ArcSinSolver extends SwingTwistBase {
    bendDir: number;
    resolve(chain: IKChain, pose: Pose, debug?: any): void;
    /** Apply ArcSolver to a slice of the chain  */
    _resolveSlice(chain: IKChain, pose: Pose, aIdx: number, bIdx: number, startPos: vec3, endPos: vec3, dir: number, pt: Transform, initRot?: quat, debug?: any): void;
}
export default ArcSinSolver;
