import type Pose from '../../armature/Pose';
import { IKChain } from './IKChain';
import IKRig from './IKRig';
declare class QuadrupedRig extends IKRig {
    hip?: IKChain;
    tail?: IKChain;
    spine?: IKChain;
    neck?: IKChain;
    head?: IKChain;
    hindLegL?: IKChain;
    hindLegR?: IKChain;
    foreLegL?: IKChain;
    foreLegR?: IKChain;
    tarsalL?: IKChain;
    tarsalR?: IKChain;
    carpalL?: IKChain;
    carpalR?: IKChain;
    constructor();
    /** Setup Chain Data & Sets Alt Directions */
    bindPose(pose: Pose): this;
    _setAltDirection(pose: any): void;
    resolveToPose(pose: any, debug?: any): void;
    applyBipedIKPose(p: any): void;
}
export default QuadrupedRig;
