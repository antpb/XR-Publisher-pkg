import { vec3, quat } from 'gl-matrix';
declare class TypePool {
    static _vec3Pool: vec3[];
    static _quatPool: quat[];
    static vec3(): vec3;
    static quat(): quat;
    static recycle_vec3(...ary: vec3[]): TypePool;
    static recycle_quat(...ary: quat[]): TypePool;
}
export default TypePool;
