import type { FrameInfo } from '../Animator';
import type Pose from '../../armature/Pose';
export declare type Lerp = typeof ELerp[keyof typeof ELerp];
export declare const ELerp: {
    readonly Step: 0;
    readonly Linear: 1;
    readonly Cubic: 2;
};
export interface ITrack {
    name: string;
    timeStampIndex: number;
    values: Float32Array;
    boneIndex: number;
    fnLerp: fnInterp<any>;
    apply(pose: Pose, fi: FrameInfo): this;
    setInterpolation(i: Lerp): this;
}
export declare type fnInterp<T> = (track: ITrack, fi: FrameInfo, out: T) => T;
