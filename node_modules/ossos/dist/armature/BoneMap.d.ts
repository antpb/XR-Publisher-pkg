import Armature from "./Armature";
declare class BoneChain {
    items: BoneInfo[];
}
declare class BoneInfo {
    index: number;
    name: string;
    constructor(idx: number, name: string);
}
declare class BoneMap {
    bones: Map<string, BoneInfo | BoneChain>;
    constructor(arm: Armature);
}
export default BoneMap;
export { BoneInfo, BoneChain };
