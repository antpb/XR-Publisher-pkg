import { vec3 } from 'gl-matrix';
declare class CurveSample {
    _totalLen: number;
    _sampleCnt: number;
    _aryLen: Array<number>;
    _aryInc: Array<number>;
    _aryPos: Array<vec3>;
    constructor(cnt?: number);
    add(pnt: vec3): void;
    set(i: number, pnt: vec3): void;
    updateLengths(): void;
    atLength(len: number, out?: vec3): vec3;
}
export default CurveSample;
