import { quat } from 'gl-matrix';
import SpringBase from './SpringBase';
declare class SpringQuat extends SpringBase {
    vel: quat;
    val: quat;
    tar: quat;
    epsilon: number;
    setTarget(v: quat, doNorm?: boolean): this;
    reset(v?: quat): this;
    update(dt: number): boolean;
}
export default SpringQuat;
